# 2026-08-21 工作日志

## Deep Research Agent 收尾

- 补全三项指标：断点恢复（恢复 18.4s/占 7%）、token 统计（单任务 21~32 万）、反思对照 ablation（3 条：省 30% 成本但命中 -0.11，如实写入 README）
- 修复：smolagents Agent 走 generate() 非 __call__（token 包装双入口）；LangGraph interrupt_before 是 invoke 关键字参数非 config 键
- 新建 ablation.py；后台任务 AKFZPm 完成通知证实"进程消失"是 tasklist 监控假象，判断完成以落盘为准
- 交付：docs/HANDOVER.md（交接文档，含 demo 三条路线方案）；仓库最新 67ab758 已推送
- 用户下一步：新窗口做 demo 打磨（推荐路线 A：checkpoint 预演 + interrupt 续跑 + demo.py 小脚本）
- 用户提问：自创 agent 能否用 WorkBuddy 积分 → 结论：不能（两套计费体系，无 API 通道），替代是免费 API/本地模型
