# 2026-08-20 工作日志

## Deep Research Agent 项目（E:\AI 应用开发\agent）

**决策**：选题=Deep Research 研究助手；模型=DeepSeek API 主 + Ollama 降级；两周完整交付。

**完成**：
- 三节点架构落地：Planner(JSON拆解) → Executor(smolagents CodeAgent) → Reflector(continue/replan/complete) → Writer，LangGraph 条件边回环 + MemorySaver 断点续跑
- 搜索方案：DuckDuckGo 国内不可达已弃用，自研 Bing 国内版 HTML 解析（lxml XPath 提取 li.b_algo，约11s/请求，免费直连），Tavily 可选
- 模型：DeepSeek API key 已配置，实测模型 id 用 `deepseek-v4-flash`（deepseek-chat 也可用但账号默认列出 v4 系列）
- CLI(main.py) + FastAPI(api.py, /health /run) + 评测(evaluate.py + eval_set/tasks.json 16条自动打分)
- 第一个 demo 跑通：巴黎奥运金牌题 → 40金27银24铜、排名第2，报告结构完整
- README + 技术博客草稿(docs/technical-blog.md) + git init 已提交首版

**踩坑记录**（已沉淀进博客）：
1. duckduckgo-search 改名 ddgs 且国内被墙；cn.bing.com 可用
2. 提示词 f-string 与 .format() 双重转义冲突 → 统一普通字符串+format，JSON 示例用 {{}} 转义
3. smolagents 1.26 无 OpenAICompatibleModel，改 OpenAIModel
4. DeepSeek 账号模型列表是 deepseek-v4-flash/pro（不是 deepseek-chat 默认）

**进行中**：16条评测集后台运行中（先跑了前5条验证）
**待办**：评测数据 → README 补指标 → 技术博客补结果 → GitHub 推送（需问用户仓库名）
